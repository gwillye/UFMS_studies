# Copie para `.env` (ou cfg/app.cfg) e ajuste. NUNCA commite o `.env` real.
SECRET_KEY=troque_por_uma_chave_aleatoria
DATABASE=postgresql://user:senha@localhost:5432/compdist
DEFAULT_USERNAME=admin
DEFAULT_PASSWORD=troque_esta_senha
